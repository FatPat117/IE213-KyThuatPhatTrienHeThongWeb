/**
 * Hàm dùng chung: format tiền tệ, kiểm tra địa chỉ ví hợp lệ, parse error...
 * Ví dụ: validator.js (isValidAddress), format.js (formatEther, formatCurrency).
 */
// const { isValidAddress } = require('./validator.js');
// const { formatCurrency } = require('./format.js');
// module.exports = { isValidAddress, formatCurrency };
