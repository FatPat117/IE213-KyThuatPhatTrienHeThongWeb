/**
 * Services: logic nghiệp vụ (controller chỉ nhận request, trả response).
 * Ví dụ: AuthService (verify chữ ký ví), CampaignService (tính toán gây quỹ),
 * ContractService (gọi Smart Contract từ server để đồng bộ dữ liệu).
 */
// const AuthService = require('./auth.service.js');
// const CampaignService = require('./campaign.service.js');
// module.exports = { AuthService, CampaignService };
